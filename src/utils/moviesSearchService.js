export default class MoviesSearchService {
  getMovies() {
    console.log("testing............")
    // return fetch('utils/movieName.js').then(res => res.json())
        // .then(d => d.data);
  }
}
